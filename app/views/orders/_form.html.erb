
<%= form_for @order, remote: true, :url => order_ajax_path do |f| %>
  <label>you can write friend email</label></br>
  <%= f.label :friends, "Email" %>:
  <%= f.text_field :order_for, list: 'friends' %>
  <%= f.submit :add %>

  <datalist id="friends">
    <% if @followables_array != nil  %>
    <% if @followables_array.size > 0 %>
      <% (@followables_array).each do |u| %>
        <option value="<%= u %>"><%= u %></option>
      <% end %>
    <% end %>
    <% end %>
  </datalist><br/><br/>
  <% end %>

<%= form_for @order, url:orders_path, html:{ multipart: true } do |f| %>

  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="container">
  <div class="field ">
    <%= f.label :order_for %><br>
    <%= f.select :order_for, options_for_select([['breakfast'], ['lunch'] , ['dinner']]) %>
  </div>
  
  <div class="field">
    <%= f.label :order_from %><br>
    <%= f.text_field :order_from %>
  </div>

  

  <div class="field">
    <%# form_for @order, :html => { :multipart => true } do |f| %>
    <%= f.label :avatar %><br>
    <%= f.file_field :avatar %>
    <%# end %>
  </div>
  
  <!-- <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div> -->
  <!-- <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

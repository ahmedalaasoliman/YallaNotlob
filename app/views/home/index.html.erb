
inside home index

<table border="1">
	<%= @order.each do |order| %>
	<tr> 
		<td> <%= order.order_for %> </td>
		<td> <%= order.order_from %> </td>
		<td> <%= order.created_at %> </td>

	 </tr>
	 <% end %>
</table>

        <% @friend.each do |friend| %>

          <tr class="success" >
<td><%= friend.name %></td>




          </tr>
      <% end %>
